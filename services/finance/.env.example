# PURIQ Finance Engine v2 — configuration surface
# Copy to .env (or export) before launch. No secrets required.

# Data lane default: "stooq" (live daily OHLCV, no key) or "fixture" (offline deterministic)
SZL_FINANCE_ORIGIN=stooq

# Bind (uvicorn flags; shown for completeness with the runbook below)
#   uvicorn app:app --host 0.0.0.0 --port 7860
